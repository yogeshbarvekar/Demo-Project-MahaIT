name: Python Flask CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Lint with flake8
      run: |
        pip install flake8
        flake8 app.py

    - name: Run Flask app (test)
      run: |
        python app.py &
        sleep 5
        curl --fail http://127.0.0.1:5000/health

    - name: Upload app.py and static site to S3
      if: success()
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
        S3_BUCKET_NAME: ${{ secrets.S3_BUCKET_NAME }}
      run: |
        pip install awscli
        aws s3 cp app.py s3://$S3_BUCKET_NAME/app.py
        aws s3 sync templates/ s3://$S3_BUCKET_NAME/static/ --acl public-read --delete
        # Optional: upload index.html root for direct static site endpoint
        aws s3 cp templates/index.html s3://$S3_BUCKET_NAME/index.html --acl public-read
